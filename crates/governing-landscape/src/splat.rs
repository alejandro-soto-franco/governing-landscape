//! 4D Gaussian splatting over three temporal keyframes
//! (pre-earthquake, post-earthquake, proposed reconstruction).

#[derive(Debug, Default)]
pub struct Splat4D {
    // TODO: parameter buffers (means, covariances, opacities, SH coeffs, time).
}
